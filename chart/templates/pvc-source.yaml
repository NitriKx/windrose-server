apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "windrose.fullname" . }}-source
  labels:
    {{- include "windrose.labels" . | nindent 4 }}
  {{- with .Values.persistence.source.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.persistence.source.accessMode }}
  storageClassName: {{ .Values.persistence.source.storageClass | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.source.size }}
