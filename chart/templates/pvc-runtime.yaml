apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "windrose.fullname" . }}-runtime
  labels:
    {{- include "windrose.labels" . | nindent 4 }}
  {{- with .Values.persistence.runtime.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.persistence.runtime.accessMode }}
  storageClassName: {{ .Values.persistence.runtime.storageClass | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.runtime.size }}
